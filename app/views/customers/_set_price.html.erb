      <section class="login-form" style="margin-top: 0px;">
<%= form_for @plan, :url => price_set_customer_path(@user), :method => :post, :html => {:id => "new-form"}, :remote => true do |f| %>
<%= render :partial => "form", :locals => {:f => f} %>
  <button type="submit" name="go" class="btn btn-lg btn-primary btn-block">Set Price</button>
}
<% end %>
</section>
